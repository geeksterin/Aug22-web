let weather = "sunny"

switch(weather) {
    case('rainy'): {
        console.log("it's a rainy day")
        break
    }
    case('sunny'): {
        console.log("it's a sunny day")
        break
    }
    default: {
        console.log("It's the same as always")
    }
}